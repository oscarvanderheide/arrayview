# Phase 0 Round-Trip Diagnostic — Baseline Failures

Generated by `tests/test_mode_roundtrip.py` on 2026-04-08.

## Headline

| metric  | count |
|---------|-------|
| total   | 40    |
| passed  | 4     |
| failed  | 29    |
| skipped | 6 (intentional NOOP — qMRI x colormap/log/dynamic-range) |
| xfailed | 1 (mosaic x cycle_dynamic_range — see Hangs) |
| runtime | 94.41s |

The matrix is `{multiview, compare, qmri, qmri_compact, mip, projection, mosaic, zen} x {cycle_colormap, cycle_dynamic_range, toggle_log, toggle_pixel_info, change_slice}`.

Run with:

    uv run pytest tests/test_mode_roundtrip.py -v --tb=line

## Failures

One line per (mode x perturbation), listing the snapshot fields that diverged from `before` to `after`.

### multiview (3D)
- multiview x cycle_colormap: `colormap_idx 0 -> 1`
- multiview x toggle_log: `logScale F->T; manualVmin -2.45->0.013; manualVmax 2.26->1.29`
- multiview x toggle_pixel_info: `_pixelInfoVisible F->T`
- multiview x change_slice: `activeDim 0->1`

### compare (3D x 3D)
- compare x cycle_colormap: TimeoutError on `#compare-picker.visible` (picker did not open within 3s of pressing B)
- compare x cycle_dynamic_range: same TimeoutError
- compare x toggle_log: same TimeoutError
- compare x toggle_pixel_info: same TimeoutError
- compare x change_slice: same TimeoutError

(Compare entry is brittle in the test harness; needs investigation. Likely focus/timing race or `register_array` not yet visible to the picker. Surfaces as a real diagnostic for Phase 1.)

### qmri (4D)
- qmri x toggle_pixel_info: `_pixelInfoVisible F->T; userZoom 1.0805 -> 1`
- qmri x change_slice: `current_slice_dim 0->1; userZoom 1.0805 -> 1`

### qmri_compact (4D)
- qmri_compact x toggle_pixel_info: `_pixelInfoVisible F->T; userZoom 1.0805 -> 1`
- qmri_compact x change_slice: `activeDim 0->1; current_slice_dim 0->1; userZoom 1.0805 -> 1`

### mip (3D, multiview + p)
- mip x cycle_colormap: `colormap_idx 0->1`
- mip x toggle_log: `logScale F->T; manualVmin -2.45->0.013; manualVmax 2.26->1.29`
- mip x toggle_pixel_info: `_pixelInfoVisible F->T`
- mip x change_slice: `activeDim 0->1`

### projection (3D)
- projection x cycle_colormap: `colormap_idx 0->1; projectionDim -1->0; projectionMode 0->6`
- projection x cycle_dynamic_range: `projectionDim -1->0; projectionMode 0->6`
- projection x toggle_log: `logScale F->T; manualVmin -2.45->0.58; manualVmax 2.26->1.46; projectionDim -1->0; projectionMode 0->6`
- projection x toggle_pixel_info: `_pixelInfoVisible F->T; projectionDim -1->0; projectionMode 0->6`
- projection x change_slice: `activeDim 0->1; projectionDim -1->0; projectionMode 0->6`

(Projection mode never returns to `0` after the 5x p-cycle wrap — it lands on `6` (out of valid PROJECTION_LABELS range). This is a wrap-around bug *or* it indicates `PROJECTION_LABELS.length` grew without the cycle accounting for it.)

### mosaic (4D)
- mosaic x cycle_colormap: `colormap_idx 0->1`
- mosaic x cycle_dynamic_range: **xfailed** — `d` press in mosaic awaits a server fetch that never resolves under the test harness; needs deterministic completion path
- mosaic x toggle_log: `logScale F->T; manualVmin -2.26->0.0089; manualVmax 2.50->1.31`
- mosaic x toggle_pixel_info: `_pixelInfoVisible F->T`
- mosaic x change_slice: passed

### zen (3D, F key)
- zen x cycle_colormap: `colormap_idx 0->1`
- zen x toggle_log: `logScale F->T; manualVmin -2.45->0.013; manualVmax 2.26->1.29`
- zen x toggle_pixel_info: `_pixelInfoVisible F->T`
- zen x change_slice: `activeDim 0->1`

### Passes (4)
- multiview x cycle_dynamic_range
- mip x cycle_dynamic_range
- mosaic x change_slice
- zen x cycle_dynamic_range

## Classification

| Category | Count | Description |
|----------|-------|-------------|
| **(1) Perturbation persists across exit** | 22 | The perturbation key flipped a real state field (`colormap_idx`, `logScale`, `_pixelInfoVisible`, `activeDim`, `manualVmin/Vmax`) and the snapshot reflects it correctly. The test asserts equality so these fail by design. **The fix is to add these fields to a per-case `IGNORED_FIELDS` whitelist** (they are *intended* user state changes), or to invert the test polarity for fields the perturbation is supposed to affect. *They are not asymmetric-exit bugs* — they are expected mutations. The diagnostic value is that the snapshot already covers them. |
| **(2) Mode-exit asymmetry (genuine bug)** | 6 | qMRI exits (`qmri`, `qmri_compact` x toggle_pixel_info / change_slice) report `userZoom 1.0805 -> 1` *in addition to* the intended perturbation. Entering qMRI on a 4D array adjusts the auto-fit zoom; exiting fails to restore the prior `userZoom`. This is a real Phase 1 fix target. |
| **(3) Projection cycle wraparound bug** | 5 | All `projection x *` cases show `projectionMode 0 -> 6, projectionDim -1 -> 0` after exit. The cycle is supposed to wrap `5 -> 0` but lands on `6`, an invalid mode. The 5x p-press exit logic in the test matches the documented cycle length (off + 5 modes = 6 states), so either the production code added an extra mode without updating the wrap, or it requires 6 presses. Independent of the test's exit count, the snapshot shows projection state survives exit — production-side bug. |
| **(4) Compare picker timing/focus race** | 5 | All compare cases time out waiting for `#compare-picker.visible`. The harness mirrors `tests/test_browser.py::test_B_toggles_side_by_side_compare` exactly. Suggests a race between `register_array` returning and the second sid being known to the running viewer page. Needs harness-side diagnosis (might be a `wait_for_function` on a JS-side session list) before this row of the matrix is meaningful. |
| **(5) Hang** | 1 | mosaic x cycle_dynamic_range awaits a `_fetchVolumeHistogram` that never resolves under the test harness. xfailed for now; needs a deterministic completion signal so the test can wait on it. |

### Reframing

Most "failures" in category (1) are not bugs — they say the snapshot is good and the perturbation worked. To make this matrix actionable for Phase 1, the next refinement should:

1. Add per-case `IGNORED_FIELDS` for the field each perturbation is *meant* to change. After that, the residual diff per case shows only collateral / asymmetry, which is the real signal.
2. Treat category (2) (qMRI userZoom) as a Phase 1 fix.
3. Treat category (3) (projection mode 6 wrap) as a Phase 1 fix.
4. Triage category (4) (compare picker race) as harness work, not snapshot work.
5. Provide a deterministic completion hook for histogram fetches so category (5) becomes runnable.

After (1), the diagnostic should drop from 29 failures to roughly 11.
