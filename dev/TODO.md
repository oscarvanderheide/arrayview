# TODO

- [ ] Align invocation and server ownership with `docs/lifecycle.md`.
- [ ] Keep local VS Code CLI launches tab-shared with one backend and last-tab shutdown.
- [ ] Keep VS Code file-click and custom-editor launches extension-owned and subprocess-based when possible.
- [ ] Keep `view(arr)` alive after script exit until viewer instances close.
- [ ] Keep Jupyter kernel-owned cleanup explicit, not tied to iframe disappearance.
- [ ] Keep remote/tunnel persistence limited to the cases that need `--serve` or direct-server behavior.
- [ ] Keep plain SSH on localhost port-forward behavior with transient shutdown.
